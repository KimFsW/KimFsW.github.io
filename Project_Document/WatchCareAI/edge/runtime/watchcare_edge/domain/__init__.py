"""Edge domain layer: frames, detections, tracks, poses, risks, envelopes.

Pure stdlib, framework-free. AI engines (YOLO / Vision / PoseNet / ONNX /
Core ML) are pluggable behind the protocols in interfaces.py (doc 4.2).
"""
