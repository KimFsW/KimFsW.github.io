"""Audit module (stub).

Responsibility: append-only audit log - who viewed / downloaded / deleted
which event, plus every lifecycle transition forwarded from the bus.
Retention follows compliance needs and is configured separately from
evidence retention (design doc 7.3).
"""
