"""Cameras module (stub).

Responsibility: camera metadata (name, room, mounting hints), stream health
snapshots relayed from the edge, snapshot/HLS ticket issuing for LiveView.
RTSP/ONVIF protocol handling stays on the edge node (design doc 3.2).
"""
